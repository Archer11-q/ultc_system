# CLAUDE.md — 高校实验室实训耗材智能管理系统

## 项目概况

基于 C 语言开发的高校实验室实训耗材智能管理系统。面向高校计算机、机电、电子类实验室，完整覆盖耗材入库、学生领用、归还、损耗报废、库存预警、实训消耗统计、耗材检索全流程。

- **仓库**：`git@github.com:Archer11-q/ultc_system.git`
- **开发环境**：Windows CLion / WSL2（GCC）
- **构建系统**：CMake ≥ 3.16，GCC ≥ 9（C11 标准）
- **语言约定**：所有文档、注释、输出使用中文
- **注释规范**：注释采用技术文档风格，面向阅读代码的开发者，禁止使用"你"等 AI 对话用语

## 核心约束

1. **纯 C 语言**：不使用 C++ 特性，遵循 C11 标准
2. **链表动态存储**：所有集合数据使用单向链表组织，不使用固定数组
3. **本地二进制文件持久化**：数据以二进制格式存入本地文件，不依赖任何数据库
4. **控制台交互**：基于文本菜单的命令行界面，无需图形界面
5. **零外部依赖**：仅使用 C 标准库和 Windows/Linux 系统 API（文件操作、时间函数）

## 项目文件结构

```
ultc_system/
├── include/                 # 头文件
│   ├── types.h              # 所有结构体定义、枚举、常量
│   ├── auth.h               # 登录认证模块
│   ├── material.h           # 耗材档案管理模块
│   ├── borrow.h             # 领用/归还/逾期模块
│   ├── inventory.h          # 库存预警/盘点模块
│   ├── search.h             # 检索模块
│   ├── stats.h              # 统计模块
│   ├── file_io.h            # 二进制文件读写模块
│   └── ui.h                 # 控制台界面/菜单/表格输出
├── src/                     # 实现文件
│   ├── main.c               # 主入口 + 主菜单路由
│   ├── auth.c               # 登录认证实现
│   ├── material.c           # 耗材管理实现
│   ├── borrow.c             # 领用归还实现
│   ├── inventory.c          # 预警盘点实现
│   ├── search.c             # 检索实现
│   ├── stats.c              # 统计实现
│   ├── file_io.c            # 文件读写实现
│   └── ui.c                 # 界面实现
├── tests/                   # 测试文件
│   ├── test_auth.c
│   ├── test_material.c
│   ├── test_borrow.c
│   ├── test_inventory.c
│   ├── test_search.c
│   ├── test_stats.c
│   └── test_file_io.c
├── data/                    # 运行时数据文件目录（二进制持久化文件）
├── CMakeLists.txt
├── .gitignore
├── CLAUDE.md
└── README.md
```

## 当前进度

| 版本 | 模块 | 状态 |
|------|------|------|
| — | 项目初始化 | 🔲 待开始 |

## 版本迭代路线图

### v0.1 — 项目骨架 + 数据结构定义 + 文件 IO 基础

**目标**：搭建可编译运行的空白项目，定义全部核心数据结构，实现二进制文件读写基础函数。

- CMake 构建系统，C11 标准，`src/` + `include/` 目录结构
- `types.h`：定义管理员结构体、耗材结构体、领用记录结构体、报废记录结构体、盘点日志结构体，以及分类/属性/角色/状态等枚举
- `file_io.h/c`：通用二进制文件读写函数
  - `file_write_all(const char* filename, const void* data, size_t elem_size, int count)` — 写入（魔数+版本+条数+数据体）
  - `file_read_all(const char* filename, size_t elem_size, int* out_count)` — 读取（校验魔数+版本，返回已分配内存的指针）
  - 文件格式：4 字节魔数 `"ULTC"` + 2 字节版本号 + 4 字节记录条数 + N × 固定大小结构体
  - 跨平台字节序处理（`<arpa/inet.h>` / `WinSock2.h` 的 `htonl`/`ntohl` 或手写转换宏）
- `main.c`：主菜单骨架（仅打印标题，各菜单项指向占位函数）
- `ui.h/c`：通用界面函数（清屏、打印分隔线、打印表格边框、获取用户输入）

**测试**：`test_file_io.c` — 写入+读取+校验 3 项基础测试

---

### v0.2 — 登录认证模块

**目标**：多管理员账号管理，密码校验，连续错误锁定，角色权限控制。

- `auth.h/c`：
  - 管理员链表加载/保存（文件：`data/admin.dat`）
  - 首次运行自动创建默认管理员账号（admin / admin123，角色=老师）
  - 登录流程：输入用户名 → 匹配查找 → 输入密码 → 校验 → 返回角色
  - 连续 3 次密码错误：锁定账号，10 秒后自动解锁
  - 角色枚举：`ROLE_ADMIN`（老师，全权限）/ `ROLE_TA`（助教，仅查询）
  - 管理员管理（仅 ROLE_ADMIN 可操作）：新增/删除/修改密码
- `ui.c`：登录界面（用户名+密码输入，密码回显为 `*`）
- `main.c`：登录→主菜单→权限过滤（助教不显示增删改菜单项）

**测试**：`test_auth.c` — 登录成功/失败/锁定/超时解锁/权限校验 5+ 项

---

### v0.3 — 耗材档案管理模块

**目标**：耗材的增删改查，分类/属性区分，分页展示。

- `material.h/c`：
  - 耗材链表加载/保存（文件：`data/material.dat`）
  - 新增耗材：编号唯一校验，重复拒绝；字段完整性校验
  - 修改耗材：按编号查找，更新库存/预警值/存放位置/单价
  - 删除耗材：按编号删除（需确认，仅 ROLE_ADMIN 可操作）
  - 分页展示：每页 10 条，区分一次性/可循环耗材标记
  - 耗材字段：编号、名称、分类（电子元器件/电工工具/开发板/化学耗材/机械零件）、属性（一次性/可循环）、采购单价、总库存、最低预警库存、存放柜号、采购日期
- `ui.c`：耗材表格打印、分页控制（上一页/下一页/退出）

**测试**：`test_material.c` — 新增/重复拒绝/修改/删除/分页 8+ 项

---

### v0.4 — 学生领用业务模块

**目标**：学生信息录入，差异化领用规则（一次性扣库存 vs 可循环仅登记），生成领用回执。

- `borrow.h/c`：
  - 领用记录链表加载/保存（文件：`data/borrow.dat`）
  - 领用流程：录入班级→学号→姓名→实训项目编号→选择耗材→输入数量
  - 规则校验：
    - 一次性耗材：库存充足才可领用，领用后**直接扣减**库存
    - 可循环耗材：领用**仅登记**，不扣减总库存
  - 生成领用单据（格式：`BORROW-YYYYMMDD-NNN`），记录领用时间、操作管理员
  - 控制台打印领用回执（含所有领用明细）
  - 同一学生可同时领用多种耗材（一次领用生成多条记录，共享同一单据号）
- `material.c`：新增 `material_reduce_stock()` 函数（一次性耗材扣减）
- `ui.c`：领用回执格式化打印

**测试**：`test_borrow.c` — 一次性领用扣库存/可循环仅登记/库存不足拒绝/回执生成 8+ 项

---

### v0.5 — 归还与逾期管理模块

**目标**：可循环耗材归还登记，7 天逾期判定，损坏转报废。

- `borrow.h/c`（扩展）：
  - 归还流程：输入学号→列出该生所有未归还记录→选择领用单号→确认归还
  - 逾期判定：当前日期 - 领用日期 > 7 天 → 标记逾期状态
  - 逾期统计：`borrow_get_overdue_list()` 返回逾期学生名单
  - 损坏登记：归还时录入损坏情况，损坏记录自动转入报废台账
  - 归还状态流转：`领用中 → 已归还` 或 `领用中 → 逾期 → 已归还` 或 `领用中 → 报废`
- `material.c`（扩展）：`material_scrap()` 报废函数 — 扣减库存 + 写入报废记录
- `material.h`：新增报废记录结构体，报废记录链表
- `file_io.c`：报废记录持久化（文件：`data/scrap.dat`）

**测试**：`test_borrow.c`（扩展）— 正常归还/逾期检测/逾期名单/损坏转报废 8+ 项

---

### v0.6 — 库存预警与盘点模块

**目标**：低库存自动预警，采购清单生成，库存盘点与差异修正。

- `inventory.h/c`：
  - 库存预警查询：遍历耗材链表，筛选 `total_stock < min_stock` 的耗材
  - 采购清单生成：预警耗材列表，计算建议采购量（`min_stock × 2 - total_stock`），估算采购金额
  - 盘点功能：
    - 遍历全部耗材，逐一显示账面库存，操作员输入实际库存
    - 差异 = 实际 - 账面，记录差异值
    - 支持手动修正（用实际值覆盖账面值）
    - 生成盘点日志（时间、操作员、耗材编号、账面值、实际值、差异）
  - 盘点日志持久化（文件：`data/stocktake.dat`）
- `ui.c`：预警表格（红色/警告标记）、采购清单表格、盘点交互界面

**测试**：`test_inventory.c` — 预警筛选/采购量计算/盘点差异/日志记录 6+ 项

---

### v0.7 — 检索模块

**目标**：耗材精准查询与模糊匹配，领用记录多条件筛选。

- `search.h/c`：
  - 耗材编号精准查询：精确匹配耗材 ID，返回单条记录
  - 耗材名称模糊匹配：子串匹配（朴素算法或 KMP），返回匹配列表
  - 领用记录筛选：按班级/学号/实训项目编号进行组合条件筛选
  - 筛选结果分页展示
- `ui.c`：搜索框提示、搜索结果分页

**测试**：`test_search.c` — 精准查询命中/未命中/模糊匹配/多条件筛选 6+ 项

---

### v0.8 — 数据统计模块

**目标**：月度消耗统计、班级排行榜、逾期/报废成本汇总。

- `stats.h/c`：
  - 月度消耗统计：按月份汇总各类耗材总消耗金额、领用总次数
  - 班级用量排行：按班级汇总耗材使用量，输出消耗排行榜（降序）
  - 逾期统计：统计当前逾期未归还工具数量、逾期学生名单
  - 报废成本汇总：统计报废耗材总成本（按耗材类型分类汇总）
  - 统计数据输出为格式化表格
- `ui.c`：统计报表打印（表头、分隔线、合计行）

**测试**：`test_stats.c` — 月度金额/次数/排行榜排序/报废成本 6+ 项

---

### v0.9 — 集成测试与全流程验证

**目标**：端到端流程测试，边界条件覆盖，数据一致性校验。

- 全流程模拟测试：
  - 登录 → 新增耗材 → 学生领用（一次性+可循环）→ 归还 → 逾期检测 → 库存预警 → 盘点 → 统计
  - 多学生并发领用，库存扣减一致性
  - 报废→库存联动验证
- 边界测试：
  - 空链表操作（无耗材时领用/归还）
  - 库存为 0 时的领用拒绝
  - 所有耗材都低于预警值时的采购清单
  - 无归还记录时的逾期统计
- 数据持久化验证：
  - 程序重启后数据完整恢复
  - 文件损坏（魔数不匹配）时的错误处理
- 所有测试使用 `assert()` + `printf()` 风格，不使用第三方测试框架

---

### v1.0 — 最终交付

**目标**：代码清理、文档完善、提交物准备。

- 代码格式化统一（clang-format）
- 全局审查：注释完整性、函数文档、边界检查
- README.md：项目说明、编译运行指南、功能清单、系统架构图
- 生成 PDF 说明文档（含截图和系统逻辑流程）
- 录制演示视频（≤10 分钟）
- 最终 `git tag v1.0` + push

## 关键设计决策

### 1. 二进制文件格式

所有持久化文件采用统一格式：

```
偏移量   大小   内容
0        4      魔数 "ULTC"（0x55 0x4C 0x54 0x43）
4        2      版本号 uint16_t（大端序）
6        4      记录条数 uint32_t（大端序）
10       N*S    N 条固定大小结构体（顺序存放）
```

- 读取时校验魔数和版本号，不匹配则报错
- 版本号用于未来结构体变更的兼容性判断
- 所有多字节整数使用大端序存储（`htonl`/`ntohl`），确保跨平台可读

### 2. 字符串存储策略

- 结构体中所有字符串字段使用固定长度 `char` 数组（如 `char name[64]`）
- 优势：结构体大小固定，二进制序列化/反序列化无需额外处理
- 代价：存在空间浪费，但实验室耗材数据量小（< 10000 条），可以接受
- 字符串操作统一使用 `strncpy`/`snprintf`，避免缓冲区溢出

### 3. 链表设计

- 所有集合（管理员、耗材、领用记录、报废记录、盘点日志）使用单向链表
- 链表节点内嵌数据字段（不分离节点和数据），减少内存分配次数
- 提供统一的链表操作：`append`（尾插）、`find_by_*`（查找）、`remove_by_*`（删除）、`free_all`（释放）
- 不单独抽象"通用链表"——每个模块根据自身结构体字段编写专用操作函数，保持代码直观

### 4. 日期时间处理

- 使用 `<time.h>` 的 `time_t` 存储时间戳（领用时间、归还时间、采购日期等）
- `struct tm` 用于日期拆分和比较
- 逾期天数计算：`difftime(now, borrow_time) / 86400.0`，与 7 天比较
- 采购日期等仅需日期精度的字段，使用 `time_t` 存储（忽略时分秒部分）
- 显示时使用 `strftime()` 格式化为 `YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS`

### 5. 权限模型

| 角色 | 枚举值 | 权限范围 |
|------|--------|----------|
| 实验老师（管理员） | `ROLE_ADMIN` | 全部功能：增删改耗材、处理领用/归还、盘点、统计、管理员管理 |
| 实训助教 | `ROLE_TA` | 查询领用记录、查看耗材、预警查询、统计查看（不含增删改操作） |

- 权限检查在菜单路由层完成：主菜单根据当前登录角色显示不同的菜单项
- 敏感操作函数内部也做二次校验（防御性编程）

### 6. 密码安全

- 密码以明文形式存储在二进制文件中（C 语言竞赛项目，不要求加密强度）
- 密码输入时控制台回显为 `*`（使用 `getch()` / `_getch()` 逐字符读取）
- 可选改进（v1.0 考虑）：简单哈希存储（如 XOR 混淆或 SHA-256）

### 7. 编码约定

- 源代码文件统一使用 UTF-8 编码
- Windows 控制台启动时执行 `system("chcp 65001 > nul")` 切换到 UTF-8
- 所有面向用户的输出文本使用中文
- 代码注释使用中文，采用技术文档语气（描述"做什么"和"为什么"），不使用"你"、"我"等人称代词

## 开发协作模式

1. **版本驱动**：严格按照 CLAUDE.md 中的版本路线图迭代，一个版本完成并推送后再开始下一个版本
2. **AI 执笔，人工检阅**：AI 负责生成全部代码、测试、文档；用户负责审阅设计决策和代码质量
3. **逐模块递进**：每个版本聚焦一个模块，独立完成 + 测试通过 + 更新 CLAUDE.md 进度表 + git commit + tag + push
4. **功能优先**：不提前优化，每个版本只实现当前版本规划的功能
5. **完成后暂停**：每个版本的全部工作（代码+测试+.gitignore+push）完成后，等待用户指令再进入下一版本

## Git 工作流

- 所有开发在 `main` 分支直推
- 每个版本完成后：
  ```
  git add .
  git commit -m "v0.X: 模块名称 — 功能概述"
  git tag v0.X
  git push origin main --tags
  ```
- `.gitignore` 在每个版本结束时更新，排除编译产物（`build/`、`*.o`、`*.exe`）、IDE 配置（`.idea/`、`cmake-build-*/`）、运行时数据文件（`data/*.dat`，首次初始化时自动生成）

## .gitignore 约定

- `build/`、`cmake-build-*/` — CMake 构建输出
- `.idea/`、`.vscode/`、`*.swp`、`*.swo` — IDE 临时文件
- `*.o`、`*.obj`、`*.exe`、`*.out` — 编译产物
- `data/*.dat` — 运行时数据文件（程序首次运行自动生成）
- PDF 说明文档和演示视频不纳入版本管理（体积大，单独提交竞赛平台）

## 测试约定

- 不使用第三方测试框架，所有测试用 `assert()` + `printf()` 手写
- 每个模块对应一个独立测试可执行文件（`test_auth`、`test_material` 等）
- CMake 中 `ultc_lib` 是静态库，所有 test target 链接它
- 测试输出格式：`[PASS]` / `[FAIL]` 前缀 + 测试描述
- 每个测试 target 返回 0（全部通过）或非 0（有失败）

## README 约定

- 中文撰写
- 包含：项目简介、功能清单、编译运行指南、系统架构图（ASCII art）、版本历史
- 每个版本迭代后更新 README 中的功能状态标记
